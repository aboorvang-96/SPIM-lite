# SPIM Lite — Supabase configuration
#
# Prefer setting expo.extra.supabaseUrl / .supabaseAnonKey in app.json so the
# values are baked into the EAS build. These env vars are a runtime fallback
# for `npx expo start` during development.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
